lints.workspace = true

[package]
name = "defuse-ckd"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
defuse-kdf-mpc.workspace = true
defuse-rand-compat = { workspace = true, features = ["rand_core_0_6"] }

# TODO: activate "zeroize" feature once this PR is released:
# https://github.com/filecoin-project/blstrs/pull/66
blstrs.workspace = true
derive_more = { workspace = true, features = ["as_ref", "from", "into"] }
near-account-id.workspace = true
pairing.workspace = true
rand_core.workspace = true
# zeroize = { workspace = true, features = ["derive"] }

arbitrary = { workspace = true, features = ["derive"], optional = true }
borsh = { workspace = true, features = ["derive"], optional = true }
bs58 = { workspace = true, optional = true }
hex-literal = { workspace = true, optional = true }
schemars = { workspace = true, features = ["derive"], optional = true }
serde = { workspace = true, features = ["derive"], optional = true }
serde_with = { workspace = true, optional = true }
thiserror = { workspace = true, optional = true }

[target.'cfg(near)'.dependencies]
near-sdk.workspace = true

[features]
fmt = ["dep:bs58", "dep:thiserror"]
serde = ["dep:serde", "dep:serde_with", "fmt"]
arbitrary = ["dep:arbitrary", "fmt"]
borsh = ["dep:borsh", "fmt"]
borsh-schema = ["borsh", "borsh/unstable__schema"]
schemars-v0_8 = ["dep:hex-literal", "dep:schemars", "serde", "serde_with?/schemars_0_8"]
abi = ["borsh-schema", "schemars-v0_8"]

[dev-dependencies]
hex-literal.workspace = true
rand = { workspace = true, features = ["sys_rng"] }
rstest.workspace = true
serde_json.workspace = true

[target.'cfg(near)'.dev-dependencies]
near-sdk = { workspace = true, features = ["unit-testing"] }
