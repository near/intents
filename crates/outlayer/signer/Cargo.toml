[package]
name = "defuse-outlayer-signer"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
defuse-kdf = { workspace = true, features = ["digest"] } # TODO: digest?

hkdf = { workspace = true, features = ["kdf"] }
sha3.workspace = true

zeroize = { workspace = true, optional = true }

[features]
ed25519 = ["defuse-kdf/ed25519"]
secp256k1 = ["defuse-kdf/secp256k1"]
zeroize = ["defuse-kdf/zeroize", "dep:zeroize", "sha3/zeroize"]

[dev-dependencies]
hex-literal.workspace = true
rstest.workspace = true
