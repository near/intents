lints.workspace = true

[package]
name = "defuse-time"
edition.workspace = true
version.workspace = true
rust-version.workspace = true
repository.workspace = true

[dependencies]
jiff = { workspace = true, features = ["std"], optional = true }

# arbitrary = { workspace = true, features = ["derive"], optional = true }
# borsh = { workspace = true, optional = true }
# cfg_eval = { workspace = true, optional = true }
# defuse-borsh-utils = { workspace = true, features = ["jiff"], optional = true }
# defuse-serde-utils = { workspace = true, features = ["time"], optional = true }
# schemars = { workspace = true, features = ["derive"], optional = true }
# serde = { workspace = true, features = ["derive"], optional = true }
# serde_with = { workspace = true, features = ["time_0_3"], optional = true }

[target.'cfg(near)'.dependencies]
near-sdk.workspace = true

[features]
jiff = ["dep:jiff"]

# # TODO: large-dates?
# abi = [
#   "borsh?/unstable__schema",
#   "defuse-borsh-utils?/abi",
#   "dep:schemars",
#   "serde_with?/schemars_0_8",
# ]
# arbitrary = ["dep:arbitrary"]
# borsh = ["dep:borsh", "dep:defuse-borsh-utils"]
# serde = [
#   "dep:cfg_eval",
#   "dep:defuse-serde-utils",
#   "dep:serde",
#   "dep:serde_with",
#   "time/serde",
# ]

[dev-dependencies]
defuse-time = { path = ".", features = ["jiff"] }

[target.'cfg(near)'.dev-dependencies]
near-sdk = { workspace = true, features = ["unit-testing"] }
