[package]
name = "defuse-test-utils"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
poa = []
defuse = []
imt = []
escrow = []
wallet = []
outlayer = []
near-contract = ["dep:defuse-core", "dep:defuse-crypto"]

[dependencies]
defuse-randomness.workspace = true

defuse-core = { workspace = true, features = ["arbitrary"], optional = true }
defuse-crypto = { workspace = true, features = ["arbitrary", "ed25519", "secp256k1", "p256"], optional = true }

arbitrary.workspace = true
rstest.workspace = true
