[package]
name = "defuse-tests"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true

[lints]
workspace = true

[features]
default = ["defuse", "poa", "escrow-swap", "long"]

# NOTE: features used in integration tests
# poa = ["defuse-sandbox/poa"]
# defuse = ["defuse-sandbox/defuse", "poa"]
# escrow-swap = ["defuse-sandbox/escrow"]
# long = []

poa = []
defuse = []
escrow-swap = []
long = []

[dependencies]
defuse-sandbox = { workspace = true, features = ["defuse", "poa", "escrow"] }

# TODO: make dependencies optional after splitting env for each contract
defuse = { workspace = true, features = ["contract", "arbitrary", "imt"]}
defuse-escrow-swap = { workspace = true, features = ["auth_call"] }
defuse-poa-factory = {workspace = true, features = ["contract"] }

defuse-near-utils = { workspace = true }
defuse-serde-utils = { workspace = true }
defuse-randomness.workspace = true
defuse-test-utils.workspace = true
near-sdk.workspace = true

futures = "0.3"
anyhow.workspace = true
arbitrary.workspace = true
bnum = { workspace = true, features = ["rand"] }
chrono.workspace = true
derive_more.workspace = true
hex-literal.workspace = true
hex.workspace = true
impl-tools.workspace = true
itertools.workspace = true
multi-token-receiver-stub = { path = "contracts/multi-token-receiver-stub" }
near-crypto.workspace = true
near-contract-standards.workspace = true
rstest.workspace = true
serde_json.workspace = true
strum.workspace = true
tokio = { workspace = true, features = ["macros"] }
tlb-ton = { workspace = true, features = ["arbitrary"] }
defuse-nep245.workspace = true
